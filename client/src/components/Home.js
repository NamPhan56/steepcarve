import styles from './home.css'

// import classNames from 'classnames';
const Home = () => {
        const content = (
            <div>
                <header>Welcome to Steep Carve!</header>

                <main>
                </main>
            </div>
        )
        return content
}

export default Home
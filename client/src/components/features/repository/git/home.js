import '../repository.css';

const home = () => {
    const content = (
        <div className="page">
            <header>Welcome to the Git Repository</header>
        </div>
    )

    return content
}

export default home